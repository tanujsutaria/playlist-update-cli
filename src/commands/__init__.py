"""Command-layer package — the destination of the main.py decomposition.

Populated incrementally by the Phase A extraction PRs (dispatch, search,
rotation, analytics, library_sync, track_ops, backfill, status_auth,
maintenance, debug). Every module added here is fully mypy-checked from
birth — never add one to the pyproject ignore list.
"""
